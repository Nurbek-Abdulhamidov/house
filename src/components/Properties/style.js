import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  flex-wrap: wrap;
  padding: var(--padding);
  gap: 20px;
  justify-content: space-between;
  /* max-width: 1440px; */
`;
